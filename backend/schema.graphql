type Buyer {
  id: ID!
  name: String! @search(by: [exact])
  age: Int
  transactions: [Transaction]
}

type Product {
  id: ID!
  name: String!
  price: Float!
}

type Transaction {
  id: ID!
  buyer: Buyer! @hasInverse(field: transactions)
  ip: String
  device: String
  date: DateTime
  products: [Product]!
}
# ======================
  name: string! @search(exact)
  age: int
  transactions: [uid]
  price: float!
  buyer: [uid]! @hasInverse(field: transactions)
  ip: string
  device: string
  date: datetime
  products: [uid]!

type Buyer {
  id
  name
  age
  transactions
}

type Product {
  id
  name
  price
}

type Transaction {
  id
  buyer
  ip
  device
  date
  products
}
